apiVersion: v1
kind: Service
metadata:
  name: python-app-svc
  namespace: microservices
  labels:
    app: python-app
spec:
  type: ClusterIP
  selector:
    app: python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
