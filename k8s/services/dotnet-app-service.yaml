apiVersion: v1
kind: Service
metadata:
  name: dotnet-app-svc
  namespace: microservices
  labels:
    app: dotnet-app
spec:
  type: ClusterIP
  selector:
    app: dotnet-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
